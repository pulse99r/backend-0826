const express = reqire('express')
const users = express.router()

const db = require('../db/dbConfig.sql')

users.get('/', (__, res) =>{
  try {
    const data = ''
    res.status(200).send 
  } catch {

  }
})